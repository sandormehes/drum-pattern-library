= Drum Pattern Library

A human-readable, Git-friendly drum-pattern library built with AsciiDoc and Antora. Version 1.0 establishes a contributor workflow with a reusable template, documented review standard, stronger validation, and a repeatable release process.

== Preview locally

You need Node.js 20 or newer. Antora reads local content through Git, so run these commands from a Git clone (or after initializing and making the first commit in a downloaded archive).

[source,shell]
----
npm install
npm run check
----

Open `build/site/index.html` in a browser.

The check command validates pattern metadata, source references, step counts, and symbols before building the site. The UI bundle lives in this repository, so building the site does not depend on downloading a remote theme.

Published pattern pages include browser-native playback with adjustable tempo and swing, General MIDI export, copyable canonical grid data, and a print-friendly practice view. No audio files or third-party playback service are required.

== Contribute a pattern

. Copy `templates/pattern-study.adoc` into the appropriate `modules/patterns/pages/` family folder.
. Give it a stable ID and filename, then add it to `modules/patterns/nav.adoc`.
. Run `npm run check` before opening a pull request.

The complete authoring and review workflow is in `CONTRIBUTING.adoc` and in the published xref:contributing.adoc[Contributing] page.

== Publish on GitHub Pages

Push the repository to GitHub and, under *Settings > Pages*, choose *GitHub Actions* as the source. Every push to `main` then builds and publishes the site. Pull requests run the same build without publishing.
