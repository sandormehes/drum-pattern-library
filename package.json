{
  "name": "drum-pattern-library",
  "version": "0.4.0",
  "private": true,
  "description": "A Git-friendly drum-pattern library published with Antora",
  "scripts": {
    "build": "antora antora-playbook.yml",
    "build:clean": "antora --clean antora-playbook.yml",
    "check:patterns": "node scripts/validate-patterns.mjs",
    "check": "npm run check:patterns && npm run build:clean"
  },
  "devDependencies": {
    "@antora/cli": "3.2.0",
    "@antora/site-generator": "3.2.0"
  },
  "engines": {
    "node": ">=20"
  }
}
